[find-itch-games](../README.md) / hasReceipt

# Function: hasReceipt()

```ts
function hasReceipt(installPath): Promise<boolean>;
```

Defined in: src/receipt.ts:194

Whether an install folder carries an itch receipt.

## Parameters

### installPath

`string`

A game's install folder.

## Returns

`Promise`\<`boolean`\>
