[find-itch-games](../README.md) / RECEIPT\_DIR

# Variable: RECEIPT\_DIR

```ts
const RECEIPT_DIR: ".itch" = ".itch";
```

Defined in: [src/receipt.ts:68](https://github.com/sparr/find-itch-games/blob/main/src/receipt.ts#L68)

The metadata directory itch keeps inside an install folder.
