[find-itch-games](../README.md) / getLaunchCandidatePaths

# Function: getLaunchCandidatePaths()

```ts
function getLaunchCandidatePaths(manifest): string[];
```

Defined in: [src/manifest.ts:86](https://github.com/sparr/find-itch-games/blob/main/node/src/manifest.ts#L86)

Absolute paths of the launch candidates butler found, best first.

## Parameters

### manifest

[`IItchAppManifest`](../interfaces/IItchAppManifest.md)

The game to resolve candidates for.

## Returns

`string`[]
