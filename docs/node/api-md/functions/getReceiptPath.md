[find-itch-games](../README.md) / getReceiptPath

# Function: getReceiptPath()

```ts
function getReceiptPath(installPath): string;
```

Defined in: [src/receipt.ts:75](https://github.com/sparr/find-itch-games/blob/main/node/src/receipt.ts#L75)

The current receipt path: `<installPath>/.itch/receipt.json.gz`.

## Parameters

### installPath

`string`

A game's install folder.

## Returns

`string`
